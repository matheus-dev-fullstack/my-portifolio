import { createTheme } from "@mui/material";

const theme = createTheme({
    pallete: {
        primary: {
            main: purple[500],
        },
        secondary: {
            main: green[500],
        }
    },
});

export default theme;
